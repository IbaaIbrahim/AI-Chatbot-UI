{
  "name": "flowdit-chatbot-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w flowdit-chatbot-library",
    "dev:demo": "npm run dev -w demo",
    "install:all": "npm install"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}